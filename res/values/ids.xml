<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- for time sheet item to put into the bundle -->
    <item name="booking_item_identity" type="id"/>
    <item name="booking_item_count" type="id"/>
    <item name="booking_item_info" type="id"/>

    <!-- for date item to put into the bundle -->
    <item name="date_item_holder" type="id"/>
    <item name="date_item_index" type="id"/>

</resources>